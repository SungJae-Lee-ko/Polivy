# DC Doc Generator 환경 설정 파일
#
# 사용 방법:
# 1. 이 파일을 .env로 복사하세요
# 2. 아래 GOOGLE_API_KEY 값을 실제 API 키로 변경하세요
# 3. DC_Doc_Generator.exe를 실행하세요
#
# ⚠️ 주의: API 키를 버전 관리 시스템에 올리지 마세요

# Google Gemini AI API 키
# 발급 방법: https://aistudio.google.com/app/apikey
# 1. 위 링크 방문
# 2. "Create API key" 클릭
# 3. "Create API key in new project" 선택
# 4. 생성된 키를 아래에 붙여넣기
GOOGLE_API_KEY=YOUR_API_KEY_HERE

# LLM 모델 선택
# 기본값: gemini-2.0-flash (권장)
# 다른 옵션: gemini-pro, gemini-pro-vision 등
GEMINI_MODEL=gemini-2.0-flash

# 임베딩 모델
# RAG 벡터 검색에 사용됨
# 변경 권장 안 함
EMBEDDING_MODEL=models/gemini-embedding-001

# 텍스트 청킹 설정
# PDF를 작은 청크로 나누는 크기
CHUNK_SIZE=1000

# 청크 겹침 크기
# 청크들 사이의 겹치는 단어 수 (문맥 연속성)
CHUNK_OVERLAP=200

# 선택사항: Retriever K값
# RAG 검색에서 반환할 관련 청크 개수
# RETRIEVER_K=5

# 로깅 레벨 (선택사항)
# DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO
